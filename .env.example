# Little Ladle Hotpot — environment variables
# Copy to .env.local for local development. Only VITE_* variables are exposed to the browser.
# The site is fully static content today, so none of these are required to run `npm run dev`.

# Public site origin used for canonical URLs, og:url and og:image (no trailing slash)
VITE_SITE_URL=https://example.com
